name: Hourly Reminder Trigger

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Cron Endpoint
        run: |
          curl -L -X GET ${{secrets.URL}} \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
